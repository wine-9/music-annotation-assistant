# 模型卡

系统使用 Discogs EffNet 音频嵌入与 MTG-Jamendo instrument 多标签分类头。输入为
mono 16 kHz 音频，输出 40 个 Sigmoid `model_score`。这些数值不是“准确率”或真实
概率。

已知限制包括：训练数据分布偏差、混音遮蔽、短暂乐器漏检、相似乐器混淆、小众
乐器标签缺失。系统通过时间聚合、父子标签、候选状态、未校准时禁用自动确认以及
人工复核降低风险。
